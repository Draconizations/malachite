import Alpine from "alpinejs";
export declare const _frames: Record<string, {
    passage: string;
    transition: boolean;
}>;
export declare const _allowNavigation: {
    back: boolean;
    forward: boolean;
};
export type MAlpine = typeof Alpine;
export default Alpine;
